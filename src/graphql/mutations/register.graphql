mutation Register($email: String!, $username: String!, $password: String!, $location: Location) {
  register(email: $email, username: $username, password: $password, location: $location) {
    ...RegularUserResponse
  }
}
